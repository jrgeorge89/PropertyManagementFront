// Este archivo se mantiene como placeholder para futuras implementaciones de React Query
// Por ahora, la gestión de estado y caché se maneja con Redux + localStorage/sessionStorage

export const queryClient = {
  // Placeholder para futuras implementaciones
}